/**
 * @class Meteor.Function
 * @summary The class `Meteor.Function` is a constructor for defining WS functions.
 */
export default class WebSocketFunction {
  /**
   * @constructor
   */
  constructor() {
    this.name = 'WebSocketFunction';
    this.type = 'function';
  }
}
